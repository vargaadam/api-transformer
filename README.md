# API-Transformer

## For local development

### Build the app

`make build`

### Start the app

`make start`

You should access the API at `localhost:3000`

> make sure you set the `.env` file

### Run tests

`make test`

### Restart the app

`make restart`

### Stop the app

`make stop`

### Reset the app

`make reset`
